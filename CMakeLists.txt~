cmake_minimum_required(VERSION 2.8)

set(PROJECT translate)

project(${PROJECT})			# Название проекта

#set (HEADERS 
#	trans.h)
	 
set (SOURCES 
	server.cpp) 
			 
add_executable (${PROJECT} ${SOURCES})
